{
	"scripts": {
		"dev": "npm run build -- --sourcemap=inline --watch",
		"build": "esbuild js/widget.js --minify --format=esm --bundle --outdir=src/anywidget_code_input/static"
	},
    "dependencies": {
        "codemirror": "^6.0.1",
        "@codemirror/language": "^6.10.1"
    },
	"devDependencies": {
		"esbuild": "^0.20.0"
	}
}
